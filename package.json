{
  "name": "tistory-dev-docs-skin",
  "version": "2.0.14",
  "description": "A Toss Tech blog style Tistory skin",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "render:local": "node scripts/render-local-skin.mjs",
    "test:local": "npm run build && npm run render:local && node tests/local-smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^7.1.0"
  },
  "dependencies": {
    "marked": "^18.0.4",
    "prismjs": "^1.30.0"
  }
}
